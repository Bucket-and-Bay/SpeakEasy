var Analysis = require('../models/analysis.model.js');
var mongoose = require('mongoose');

var updateVideo = function(req, res) {
 //find in mongoose by videoId and change isPrivate property;
};

module.exports = {
  updateVideo: updateVideo
}
